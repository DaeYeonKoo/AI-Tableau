# 대시보드 요구사항 — SL Corporation 품질 클레임 대시보드

> `Context Brief.md` / `Data Dictionary.md`를 기반으로 작성된 대시보드 스펙 문서입니다.
> 다음 단계(HTML 기획안 생성 → `.twb` 구현)의 입력값으로 사용합니다.

## 0. 개요

| 항목 | 내용 |
|---|---|
| 대시보드명 | SL Corporation 품질 클레임 대시보드 |
| 데이터 소스 | `data/sl_corporation_quality_claims.csv` (2,200건, 2023-01-01 ~ 2025-12-31) |
| 기준일(As-Of) | 2025-12-31 |
| 페이지 수 | 3장 |
| 대상 사용자 | 1) 품질경영팀 실무자(원인 추적·처리 효율) 2) 경영진(전사 현황 요약 보고) |
| 참고 스타일 | 첨부된 Hyundai Glovis 대시보드의 구조 언어(기간별 KPI 카드, 이동평균 트렌드, 버블맵+랭킹, 소형멀티플, 히트맵)를 차용하되 색상·브랜드는 별도 정의 |

### 공통 헤더 요소 (전 페이지 공통)

- 좌측 상단: 대시보드 타이틀 + 현재 페이지명
- 우측 상단: 기준일(2025-12-31), 데이터 기간(최근 12개월 등 페이지별 표기)
- 좌측 내비게이션: 1) Executive Summary 2) 원인 드릴다운 3) 리드타임 & 효율

### 전역 계산 필드 (여러 페이지에서 공통 사용)

| 계산 필드명 | 정의 | 비고 |
|---|---|---|
| `lead_time_to_receive` | `claim_received_date - occurrence_date` (일수) | 발생→접수 소요일 |
| `lead_time_to_confirm` | `claim_confirmed_date - claim_received_date` (일수) | 접수→확정 소요일. `claim_confirmed_date`가 빈값이면 NULL |
| `total_cycle_time` | `claim_confirmed_date - occurrence_date` (일수) | 발생→확정 전체 소요일 |
| `is_open` | `claim_status` IN (접수, 조사중) → TRUE | 미결 여부 플래그 |
| `is_confirmed_liable` | `claim_status` IN (확정, 보상완료) → TRUE | 실질 인정 클레임 여부 (기각 제외) |

---

## 1페이지 — Executive Summary (전사 클레임 현황 요약)

**목적**: 경영진이 한 화면에서 전체 클레임 규모·추세·핫스팟(국가/고객사/결함유형)을 파악.

### 1-1. 기간별 KPI 카드 (상단, 5칸)

참고자료의 "단기/월간/분기/연간" 카드 구조를 차용. 기준일(2025-12-31) 기준 아래 5개 기간의 **클레임 건수**와 **클레임 금액 합계(USD)**를 나란히 표시, 각 카드 하단에 직전 동기간 대비 증감률(%) 표기.

| 카드 | 기간 |
|---|---|
| 최근 1개월 | 2025-12 |
| 최근 3개월 | 2025-10 ~ 2025-12 |
| 최근 6개월 | 2025-07 ~ 2025-12 |
| 최근 12개월 | 2025-01 ~ 2025-12 |
| 전체(3개년) | 2023-01 ~ 2025-12 (강조 카드, 참고자료의 진한 색 카드와 동일한 톤) |

측정값: `COUNT(claim_id)`, `SUM(claim_amount_usd)`

### 1-2. 월별 클레임 건수·금액 추이 (좌측 중단, 콤보 차트)

- X축: 월(occurrence_date 기준, 2023-01 ~ 2025-12, 36개월)
- 막대: 월별 클레임 건수 (`COUNT(claim_id)`)
- 라인: 월별 클레임 금액 합계 (`SUM(claim_amount_usd)`), 3개월 이동평균 라인 토글 옵션
- 연도 구분선/음영으로 2023/2024/2025 구간 구분 (참고자료의 월 구간 표기 방식 참고)

### 1-3. 국가별 클레임 버블맵 (좌측 하단)

- 좌표: `claim_country`(고객사 소재국) 기준 버블
- 버블 크기: `SUM(claim_amount_usd)`
- 버블 색상: 표준편차 기준 상/하 구간 강조 (참고자료 방식) 또는 `AVG(total_cycle_time)`로 색상 인코딩 — 금액과 처리속도를 동시에 못 보여주면 크기=금액, 색=평균 리드타임 초과 여부로 확정
- 하단/우측 텍스트: "클레임 금액이 가장 큰 국가는 **{국가}** ({금액})", "건수가 가장 많은 국가는 **{국가}** ({건수}건)"

### 1-4. Top 5 랭킹 (우측, 2단)

- Top 5 고객사 — 클레임 금액 기준 가로 막대 (`customer` × `SUM(claim_amount_usd)`)
- Top 5 결함유형 — 클레임 금액 기준 가로 막대 (`defect_type_en` × `SUM(claim_amount_usd)`)

---

## 2페이지 — 원인 드릴다운 (고객사 · 생산공장 · 부품)

**목적**: "어디서(생산공장) · 무엇이(부품/결함유형) · 누구에게(고객사) 문제가 생기는가"를 실무자가 파고들 수 있게 함.

### 2-0. 전역 필터 (페이지 상단, 대시보드 전체 액션 필터)

`customer`, `production_plant`, `part_category`, 기간(occurrence_date 범위 슬라이더)

### 2-1. 부품 카테고리별 소형 멀티플 (상단, 5분할)

참고자료 Global Port Traffic의 Traffic/Empty/Import/Export 4분할 구조 차용. `part_category` 5종(Lamp Systems / Mirror Systems / Chassis Systems / Front End Module / Electrification)을 각각 소형 라인 차트로 분할:

- 각 카드: 최근 6개월 월별 클레임 금액 추이 + 최근월 전월대비 증감률(%) 라벨
- 카드 클릭 시 하단 상세 영역이 해당 카테고리로 필터링(액션 필터)

### 2-2. 생산공장 × 결함유형 매트릭스/히트맵 (좌측 중단)

- 행: `production_plant` (17개), 열: `defect_type_en` (10개)
- 셀 값: `COUNT(claim_id)` 또는 `SUM(claim_amount_usd)` (토글), 색상 농도로 인코딩
- 어느 공장이 어떤 결함 유형에 취약한지 한눈에 파악

### 2-3. 부품 카테고리별 클레임 금액 랭킹 (우측 중단)

- `part_category` × `SUM(claim_amount_usd)` 가로 막대, 내림차순

### 2-4. 고객사별 클레임 금액 구성 (하단)

- `customer` × `SUM(claim_amount_usd)` 트리맵 또는 막대. 고객사 클릭 시 2-1·2-2·2-3이 해당 고객사로 필터링

---

## 3페이지 — 리드타임 & 처리 효율 (품질대응 프로세스 진단)

**목적**: 이 데이터셋 고유의 프로세스형 날짜 구조(생산→납품→발생→접수→확정)를 활용해 클레임 처리 효율과 병목을 진단. 참고자료에는 없는, 이 프로젝트만의 차별 페이지.

### 3-1. 클레임 상태 현황 (좌측 상단)

- `claim_status` 5종(접수/조사중/확정/기각/보상완료) 건수 도넛 또는 막대
- 미결(`is_open` = TRUE) 건수·비중 강조 표시 (참고자료의 강조 카드 톤 차용)

### 3-2. 리드타임 분포 (중단, 히스토그램/박스플롯)

- `lead_time_to_receive`(발생→접수), `lead_time_to_confirm`(접수→확정) 두 지표를 나란히 분포 표시
- 전체 평균/중앙값 라인 오버레이

### 3-3. 고객사별 · 생산공장별 평균 리드타임 비교 (하단, 2단 막대)

- `customer` × `AVG(total_cycle_time)` 가로 막대 — 어느 고객사向 클레임이 처리에 오래 걸리는지
- `production_plant` × `AVG(total_cycle_time)` 가로 막대 — 어느 생산공장 이슈가 처리에 오래 걸리는지

### 3-4. 심각도(Severity)별 분포 (우측)

- `severity`(Critical/Major/Minor) × 건수, 금액 비중 — 스택 막대 또는 사이드바 차트

---

## 다음 단계

이 문서를 기반으로 HTML 기획안(목업)을 생성한 뒤, Claude Code로 핸드오프하여 `.twb` 파일을 구현합니다 (`Context Brief.md` 6장 프로세스 참고).
